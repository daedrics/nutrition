import { HealthLabelPipePipe } from './health-label-pipe.pipe';

describe('HealthLabelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HealthLabelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
